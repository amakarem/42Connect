<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Welcome {{ app.user.displayName }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f2f4f7;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #2c3e50;
            color: #ecf0f1;
            padding: 1rem 2rem;
            text-align: center;
        }

        .container {
            max-width: 900px;
            margin: 2rem auto;
            padding: 2rem;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        }

        .user-info {
            display: flex;
            align-items: center;
            gap: 2rem;
            margin-bottom: 2rem;
        }

        .user-info img {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid #2c3e50;
        }

        .user-details h2 {
            margin: 0 0 0.5rem 0;
        }

        .user-details p {
            margin: 0.25rem 0;
            color: #555;
        }

        .projects, .campus {
            margin-top: 1rem;
        }

        h3 {
            margin-bottom: 0.5rem;
            color: #2c3e50;
        }

        .logout {
            text-align: right;
            margin-top: 2rem;
        }

        .logout a {
            text-decoration: none;
            color: #fff;
            background-color: #e74c3c;
            padding: 0.5rem 1rem;
            border-radius: 5px;
        }

        .logout a:hover {
            background-color: #c0392b;
        }
    </style>
</head>
<body>
    <header>
        <h1>Welcome to 42 Connect</h1>
    </header>

    <div class="container">
        <div class="user-info">
            {% if app.user.image %}
                <img src="{{ app.user.image }}" alt="{{ app.user.displayName }}">
            {% else %}
                <img src="https://via.placeholder.com/120" alt="User Image">
            {% endif %}

            <div class="user-details">
                <h2>{{ app.user.displayName }}</h2>
                <p><strong>Email:</strong> {{ app.user.email }}</p>
                <p><strong>Intra Login:</strong> {{ app.user.intraLogin }}</p>
                <p><strong>Kind:</strong> {{ app.user.kind }}</p>
                <p><strong>Location:</strong> {{ app.user.location }}</p>
            </div>
        </div>

        <div class="projects">
            <h3>Projects</h3>
            {% if app.user.projects is not empty %}
                <ul>
                    {% for project in app.user.projects %}
                        <li>{{ project.name ?? project }}</li>
                    {% endfor %}
                </ul>
            {% else %}
                <p>No projects available.</p>
            {% endif %}
        </div>

        <div class="campus">
            <h3>Campus</h3>
            {% if app.user.campus is not empty %}
                <ul>
                    {% for c in app.user.campus %}
                        <li>{{ c.name ?? c }}</li>
                    {% endfor %}
                </ul>
            {% else %}
                <p>No campus info available.</p>
            {% endif %}
        </div>

        <div class="logout">
            <a href="{{ path('app_logout') }}">Logout</a>
        </div>
    </div>
</body>
</html>
